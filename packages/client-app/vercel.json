{
    "outputDirectory": "dist",
    "framework": "vite",
    "buildCommand": "echo '----------------------------------------' && echo 'VERIFYING_ROOT_DIR: I am reading packages/client-app/vercel.json' && echo 'Current Directory:' && pwd && echo '----------------------------------------' && npm run build"
}